# Legacy: custom bead files from beadsync v1
beads/

# bd (Beads CLI) — binary Dolt database, never commit
# Commit .beads/issues.jsonl instead (auto-exported by beadsync)
.beads/dolt/
.beads/*.db

# macOS
.DS_Store

# Editors
.idea/
*.swp
*.swo
